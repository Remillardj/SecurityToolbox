# BSOT Secret Scanning
name: Secret Scan

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install BSOT
        run: pip install -e .
      
      - name: Run secret scan
        # Fail on the exit code alone. Matched secrets must never reach the
        # public Actions log or an uploaded artifact: on a public repo those
        # copies outlive any history rewrite that scrubs the leak itself.
        # The baseline suppresses known-acceptable matches (the scanner's own
        # detection patterns, documented placeholders); refresh it with
        # `bsot file cred-baseline . -o .secret-scan-baseline.json`.
        run: |
          if ! bsot file cred-scan . --baseline .secret-scan-baseline.json --json > /dev/null 2>&1; then
            echo "::error::Secret scan found new matches. Run 'bsot file cred-scan . --baseline .secret-scan-baseline.json' locally to see them."
            exit 1
          fi
